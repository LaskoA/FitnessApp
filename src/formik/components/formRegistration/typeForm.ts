export interface TypeForm {
  email: string;
  password: string;
  first_name: string;
  last_name?: string;
  base64?: string;
};

export interface TypeFormSnake {
  email: string;
  password: string;
  firstName: string;
  lastName?: string;
  base64?: string;
};
