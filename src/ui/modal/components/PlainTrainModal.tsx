import { Dialog, Button, Typography, Box } from '@mui/material';

export interface PlainTrainModalProps {
  readonly open: boolean;
}

export const PlainTrainModal = ({ open }: PlainTrainModalProps) => {
  return (
    <Dialog open={open}>
      asd
    </Dialog>
  );
};
