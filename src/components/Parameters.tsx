import { Box } from '@mui/material';

import { LeftMenu } from '@app/app/components/LeftMenu';

export const Parameters = () => {
  return (
    <LeftMenu>
      <Box px={{ md: 6 }}>parameters page</Box>
    </LeftMenu>
  );
};
