import { Box, Typography } from '@mui/material';

import { LeftMenu } from '@app/app/components/LeftMenu';

export const ExercisesList = () => {

  return (
    <LeftMenu>
      <Box px={{ md: 6 }}>
        excersices list page
      </Box>
    </LeftMenu>
  );
};
